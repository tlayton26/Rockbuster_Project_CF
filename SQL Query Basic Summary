A common SQL query to get descriptive statistics of various variables. 

SELECT MIN(rental_rate) AS min_rent,			
    	MAX(rental_rate) AS max_rent,		
    	   AVG(rental_rate) AS avg_rent,		
	   MIN(rental_duration) AS min_dur,		
	   MAX(rental_duration) AS max_dur,		
	   AVG(rental_duration) AS avg_dur,		
	   MIN(length) AS min_length,		
	   MAX(length) AS max_length,		
	   AVG(length) AS avg_length,		
	   MIN(replacement_cost) AS min_replace,		
	   MAX(replacement_cost) AS max_replace,		
	   AVG(replacement_cost) AS avg_replace,		
	   COUNT(rental_rate) AS count_rent_values,		
       COUNT(*) AS count_rows			
FROM film;			
